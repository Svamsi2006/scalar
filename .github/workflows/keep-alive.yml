name: Streamlit Keep-Alive
on:
  schedule:
    - cron: "*/10 * * * *" # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -sL https://your-app-name.streamlit.app > /dev/null