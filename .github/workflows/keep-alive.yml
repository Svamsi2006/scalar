name: Streamlit Keep-Alive
on:
  schedule:
    - cron: "*/10 * * * *" # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s --head https://scalarproject.streamlit.app || true
